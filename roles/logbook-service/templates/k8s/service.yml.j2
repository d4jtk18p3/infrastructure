---
apiVersion: v1
kind: Service
metadata:
  name: {{ frontend_manifest }}
  namespace: {{ service_namespace }}
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: {{ frontend_manifest }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ logbook_manifest }}
  namespace: {{ service_namespace }}
spec:
  ports:
    - port: 80
      targetPort: 5001
  selector:
    app: {{ logbook_manifest }}